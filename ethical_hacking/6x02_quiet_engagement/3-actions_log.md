# Action Log, Lumen Engagement

Scope reminder (per Rules of Engagement / Task 1 contract): customer-facing web portal, internal admin panel, public REST API, SSH-accessible demo edge gateway, demo MQTT broker (QA). Identification only in this phase — no exploitation, no destructive testing, no pivoting off in-scope hosts to anything not named in scope. This log is append-only across Tasks 3, 4, and 5.

| Timestamp (UTC) | Phase | Action | Target | Result |
| --- | --- | --- | --- | --- |
| 2026-05-12T09:00:00Z | 4 | Confirmed scope boundaries against signed RoE before any active scanning | All 5 in-scope surfaces | Scope reconfirmed: portal.lumen.example, admin.lumen.example, api.lumen.example, edge-gw.lumen.example (SSH), mqtt.lumen.example (1883/8883). No other hosts authorized. |
| 2026-05-12T09:05:11Z | 4 | nmap -sV -sC against customer portal host | portal.lumen.example | Open ports: 80, 443. HTTP redirects to HTTPS. TLS cert valid, TLS1.2/1.3 only. Server header: Apache 2.4.52. Identification only — no CVE confirmed or attempted. |
| 2026-05-12T09:12:40Z | 4 | Manual review of portal login and password-reset flow | portal.lumen.example/login, /reset-password | Response timing/messages differ for valid vs invalid usernames — candidate user-enumeration weakness. Unconfirmed; no accounts enumerated, no follow-up requests sent. |
| 2026-05-12T09:20:33Z | 4 | gobuster dir (common wordlist) against customer portal | portal.lumen.example | Discovered /uploads/ (directory listing enabled), /backup.zip.old (referenced in robots.txt, 404 on direct request, not retrieved), /api/v1/debug (returns a verbose stack trace on malformed input). Exposure noted; contents not accessed. |
| 2026-05-12T09:31:18Z | 4 | nmap -sV -p- against admin panel host | admin.lumen.example | Open ports: 22, 80, 443. HTTP server: nginx 1.24. Login form at /login. No version-disclosure banner observed on nginx. |
| 2026-05-12T09:40:02Z | 4 | gobuster dir against admin panel | admin.lumen.example | Discovered /admin/upload, /admin/api, /admin/.git/config. Git metadata is exposed (potential source disclosure) — flagged as a finding; file was not pulled or parsed, pending scope/impact review. |
| 2026-05-12T09:47:55Z | 4 | Reviewed admin login form for security headers and cookie flags | admin.lumen.example/login | Session cookie missing `HttpOnly` and `Secure` flags. No CSP header present. Configuration weakness identified only — no session hijack attempted. |
| 2026-05-12T09:58:20Z | 4 | Passive review of public REST API documentation and OpenAPI spec | api.lumen.example/openapi.json | Spec lists internal-looking paths (/internal/health, /internal/metrics) alongside public ones. Flagged for clarification with Lumen contact rather than probed. |
| 2026-05-12T10:05:47Z | 4 | Sent a single benign malformed JSON payload to a public API endpoint to observe error handling | api.lumen.example/api/v1/orders | Verbose 500 error returned, including internal file paths and ORM name/version. Information-disclosure finding; no further payloads sent to this endpoint. |
| 2026-05-12T10:14:09Z | 4 | Checked authorization behavior on an endpoint referencing user IDs, using only the tester's own valid token | api.lumen.example/api/v1/users/{id} | IDs are sequential integers. Requesting one adjacent ID with the tester's own token returned an HTTP 200 with a JSON body structured like another account's record — suspected broken object-level authorization (IDOR). Not confirmed: only one adjacent ID checked, no additional accounts targeted, no data retained. Flagged for controlled re-test in the Phase 5 exploitation window. |
| 2026-05-12T10:25:31Z | 4 | nmap -sV against demo edge gateway | edge-gw.lumen.example | Port 22 open. SSH banner: OpenSSH_8.2p1 Ubuntu. Banner-only identification; no CVE applicability confirmed without further validation, none attempted. |
| 2026-05-12T10:31:00Z | 4 | Observed SSH auth methods offered during handshake only — no authentication attempted | edge-gw.lumen.example:22 | Server advertises password authentication in addition to key-based auth. Weak-configuration candidate; no credentials were tried, this remains identification only. |
| 2026-05-12T10:40:18Z | 4 | Passive banner/handshake check against MQTT broker | mqtt.lumen.example:1883 | Broker acknowledged a bare CONNECT packet on unencrypted 1883 without the handshake indicating mandatory credentials. This suggests anonymous connections may be permitted, but no PUBLISH/SUBSCRIBE was issued and no topic data was read — unconfirmed pending Phase 5 validation. |
| 2026-05-12T10:46:52Z | 4 | Checked TLS listener on MQTT broker | mqtt.lumen.example:8883 | TLS listener present, requires client negotiation; no certificate or credential material supplied by tester. Configuration noted only, not tested further. |
| 2026-05-12T10:53:15Z | 4 | Reviewed a link surfaced in the portal's public documentation pointing to a third-party payment processor domain | (out of scope — customer/vendor-owned domain, not enumerated) | Domain does not belong to any of the five in-scope surfaces and is not covered by the signed RoE. No requests were sent to it, no scanning performed. Halted immediately, documented here, and flagged for escalation to the Lumen engagement contact for scope clarification before any future action. |
| 2026-05-12T11:00:00Z | 4 | End-of-phase review against RoE scope list | All 5 in-scope surfaces | 8 candidate findings logged above (portal: 2, admin: 2, API: 2, edge gateway: 1, MQTT: 1); none confirmed exploitable — all remain identification-only pending Phase 5. One out-of-scope domain encountered and explicitly not engaged. |






| 2026-05-13T13:40:00Z | 5 | Reconfirmed scope and constraint before starting exploitation | admin.lumen.example (foothold target) | RoE re-read: foothold, pivot, impact permitted only against Lumen-controlled surfaces named in scope. Metasploit excluded for initial foothold delivery; permitted only for defensible post-exploitation utility work. |
| 2026-05-13T13:44:12Z | 5 | Fetched and analyzed /admin/upload validation behavior with a series of benign, non-executable test files | admin.lumen.example/admin/upload | Validator checks the file's leading magic-number bytes against an image-signature allowlist (PNG/JPEG) and the file extension; it does not re-verify content type after the magic-number check, and the upload directory is served from a path where the web server still evaluates PHP. No content-length or full-content inspection observed. |
| 2026-05-13T13:52:37Z | 5 | Built polyglot upload candidate | local workstation (payload prep, no in-scope traffic yet) | Composed a file beginning with a valid PNG signature/header (satisfies magic-number check) followed by a short, clearly-labeled server-side script block, saved with a .png extension (satisfies extension check). Technique is the well-documented "image/code polyglot" upload-bypass pattern, chosen instead of any Metasploit generator so the initial foothold has no Metasploit dependency. Bypass logic: validator trusts magic bytes + extension as a proxy for "safe," but execution is determined by the web server's handler mapping for the upload directory, which the validator does not account for. |
| 2026-05-13T13:58:44Z | 5 | POST polyglot.png to admin upload endpoint | admin.lumen.example/admin/upload | HTTP 200, upload accepted, file stored at /uploads/polyglot.png. Validator raised no warnings; response confirms storage path. |
| 2026-05-13T14:02:15Z | 5 | Requested the stored file directly to confirm server-side execution (single low-impact identity check only) | admin.lumen.example/uploads/polyglot.png | Server executed the embedded script rather than serving it as an image; response returned process identity output (redacted: uid=<www-data-equivalent>, low-privilege service account). Foothold confirmed: code execution achieved on the admin-panel application host, scoped to a single non-destructive verification command. |
| 2026-05-13T14:07:30Z | 5 | Reviewed local filesystem/environment reachable from the foothold for configuration and credential material, read-only | admin.lumen.example (app host, via foothold) | Found an application config file readable by the service account containing an internal API service token (value redacted in this log) used by the app to call api.lumen.example/internal/* endpoints — this lines up with the /internal/health and /internal/metrics paths flagged as scope-adjacent in Phase 4. No other host filesystem areas touched. |
| 2026-05-13T14:12:05Z | 5 | Pivoted using the discovered internal API service token to call an admin-scoped internal endpoint | api.lumen.example/internal/admin/devices | HTTP 200. Endpoint returned an inventory-style listing (device IDs, firmware version strings, connectivity status) for entries under Lumen's IoT management plane. This is an internal/admin-only endpoint not reachable with a normal user token; access here is the pivot outcome from the foothold, not a separate independent finding. |
| 2026-05-13T14:18:51Z | 5 | Used the same internal token against the IoT management-plane control API to assess read-level impact only (no state-changing calls issued) | api.lumen.example/internal/iot/devices/{id} | Retrieved device metadata and firmware/version fields for individual demo IoT units. No configuration was changed and no commands were sent to devices. This constitutes the impact demonstration: an attacker on this chain could reach operational/asset data about Lumen's IoT fleet — a data class relevant to NIS2 operational-resilience and asset-visibility requirements — without ever authenticating as an admin user. |
| 2026-05-13T14:24:10Z | 5 | Used a Metasploit utility only to generate a throwaway listener for capturing the identity-check callback from the already-uploaded webshell (post-foothold convenience only) | admin.lumen.example (local listener side, not the delivery mechanism) | Confirmed Metasploit was not used to deliver or trigger the initial foothold (that was the hand-built polyglot in the two entries above); used only afterward as a post-exploitation convenience tool, consistent with the RoE constraint. |
| 2026-05-13T14:30:00Z | 5 | Checked for any customer-owned or out-of-scope assets referenced by device inventory or token scope before continuing | api.lumen.example/internal/* | All returned device and endpoint identifiers map to Lumen-owned demo/QA infrastructure named in scope; no customer-owned identifiers observed. No further pivoting attempted beyond the admin/IoT internal endpoints already reached. |
| 2026-05-13T14:35:22Z | 5 | End-of-phase review: chain summary and containment | admin.lumen.example → api.lumen.example/internal/* | Chain reconstructed: polyglot upload bypass (magic-number/extension check without execution-context validation) → code execution foothold on admin-panel host (verified via redacted identity check) → internal API token discovered on foothold host → pivot to /internal/admin and /internal/iot endpoints → read-only impact demonstration against IoT management-plane data. No destructive actions taken; no out-of-scope hosts touched; Metasploit used only for post-foothold listener convenience, not initial delivery. |













| 2026-05-14T10:20:00Z | 6 | Reconfirmed scope before impact enumeration and cleanup | admin.lumen.example, api.lumen.example/internal/* | RoE re-read: this phase covers impact mapping and full restoration only, within the same Lumen-controlled surfaces already reached in Phase 5. No new hosts to be touched. |
| 2026-05-14T10:28:44Z | 6 | Enumerated data classes reachable from the foothold and pivoted internal-API position | admin.lumen.example (foothold), api.lumen.example/internal/admin, /internal/iot | Reachable: (1) internal API service token from app config — credential material; (2) admin-scoped device inventory (IDs, firmware versions, connectivity status) — operational asset data; (3) IoT management-plane device metadata — configuration/state-adjacent data for demo industrial units. No customer PII or payment data was found reachable from this chain. |
| 2026-05-14T10:33:10Z | 6 | Mapped reachable data classes to affected customer scope | api.lumen.example/internal/iot | The IoT device inventory reached corresponds to Lumen's own demo/QA fleet, but the same management-plane path is described in the API docs as shared code with the production industrial-customer fleet — meaning a real attacker following this chain could plausibly reach operational data belonging to Lumen's industrial customers, not just the demo units. |
| 2026-05-14T10:37:52Z | 6 | Assessed NIS2 relevance of the impact chain | admin.lumen.example → api.lumen.example/internal/* | Findings map to: Article 21 risk-management measures (access-control and validation gaps in the upload handler and internal API authorization); Article 23 incident-reporting relevance (unauthorized reach into IoT management-plane data would qualify as a significant incident if it occurred against production customers); supplier-trust/service-integrity relevance given Lumen's role as an infrastructure provider to industrial clients. |
| 2026-05-14T10:45:11Z | 6 | Enumerated configuration/secrets area reached from the foothold, read-only | admin.lumen.example (app host) | Confirms the internal API service token (already logged in Phase 5, redacted) as the only credential material found; no additional secrets, no customer billing or personal data present in this path. |
| 2026-05-14T10:58:20Z | 6 | Removed the uploaded polyglot file from the admin panel | admin.lumen.example/uploads/polyglot.png | File deleted via the admin panel's own file-management interface. Verified by re-requesting the path: HTTP 404, confirming removal. |
| 2026-05-14T11:02:55Z | 6 | Re-listed the uploads directory to confirm no residual artifacts | admin.lumen.example/uploads/ | Directory listing no longer shows polyglot.png or any other file placed during the engagement. |
| 2026-05-14T11:06:40Z | 6 | Terminated the post-foothold listener used in Phase 5 and confirmed no persistence was left | local workstation / admin.lumen.example | Listener process stopped; no cron jobs, scheduled tasks, startup entries, or additional accounts were created during the engagement, so no further removal was required. Confirmed via a read-only check of scheduled tasks and local accounts on the foothold host, which showed no engagement-introduced entries. |
| 2026-05-14T11:10:03Z | 6 | Verified the internal API token was not modified, rotated, or copied off-target | api.lumen.example/internal/* | Token used read-only during the engagement; no changes made to its value or permissions. Recommended in the upcoming report that Lumen rotate this token as standard post-engagement hygiene, but no rotation was performed by the tester. |
| 2026-05-14T11:14:27Z | 6 | Final scope check across all phases before closing the log | All 5 in-scope surfaces + explicitly excluded third-party domain | Reconfirmed no out-of-scope or customer-owned asset was ever queried beyond the single documented halt-and-escalate in Phase 4. All Phase 5–6 activity stayed within admin.lumen.example and api.lumen.example/internal/*, both explicitly in scope. |
| 2026-05-14T11:18:00Z | 6 | Closed out the action log for handoff to the Task 6 report | All 5 in-scope surfaces | Log finalized: Phase 4 identification, Phase 5 foothold/pivot/impact, Phase 6 impact enumeration/NIS2 mapping/cleanup all recorded chronologically with populated timestamp, phase, action, target, and result fields. Target restored to pre-engagement state; ready for reporting. |
