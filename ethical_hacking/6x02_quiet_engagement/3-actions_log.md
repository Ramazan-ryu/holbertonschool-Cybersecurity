# Action Log, Lumen Engagement

Scope reminder (per Rules of Engagement / Task 1 contract): customer-facing web portal, internal admin panel, public REST API, SSH-accessible demo edge gateway, demo MQTT broker (QA). Identification only in this phase — no exploitation, no destructive testing, no pivoting off in-scope hosts to anything not named in scope.

| Timestamp (UTC) | Phase | Action | Target | Result |
| --- | --- | --- | --- | --- |
| 2026-05-12T09:00:00Z | 4 | Confirmed scope boundaries against signed RoE before any active scanning | All 5 in-scope surfaces | Scope reconfirmed: portal.lumen.example, admin.lumen.example, api.lumen.example, edge-gw.lumen.example (SSH), mqtt.lumen.example (1883/8883). No other hosts authorized. |
| 2026-05-12T09:05:11Z | 4 | nmap -sV -sC against customer portal host | portal.lumen.example | Open ports: 80, 443. HTTP redirects to HTTPS. TLS cert valid, TLS1.2/1.3 only. Server header: Apache 2.4.52 (identification only — no CVE confirmed exploitable). |
| 2026-05-12T09:12:40Z | 4 | Manual review of portal login and password-reset flow | portal.lumen.example/login, /reset-password | Password-reset endpoint returns different response times/messages for valid vs invalid usernames — candidate user-enumeration issue, unconfirmed. |
| 2026-05-12T09:20:33Z | 4 | gobuster dir (common wordlist) against customer portal | portal.lumen.example | Discovered /uploads/ (directory listing enabled), /backup.zip.old (404 but referenced in robots.txt), /api/v1/debug (returns verbose stack trace on malformed input). |
| 2026-05-12T09:31:18Z | 4 | nmap -sV -p- against admin panel host | admin.lumen.example | Open ports: 22, 80, 443. HTTP server: nginx 1.24. Login form at /login. No obvious version-disclosure banner on nginx. |
| 2026-05-12T09:40:02Z | 4 | gobuster dir against admin panel | admin.lumen.example | Discovered /admin/upload, /admin/api, /admin/.git/config (git metadata exposed — potential source disclosure, not yet pulled/exploited pending scope check on downstream data). |
| 2026-05-12T09:47:55Z | 4 | Reviewed admin login form for security headers and cookie flags | admin.lumen.example/login | Session cookie missing `HttpOnly` and `Secure` flags. No CSP header present. Identification only — no session hijack attempted. |
| 2026-05-12T09:58:20Z | 4 | Passive review of public REST API documentation and OpenAPI spec | api.lumen.example/openapi.json | Spec exposes internal-looking endpoints (/internal/health, /internal/metrics) alongside public ones — scope-adjacent, flagged for clarification rather than probed further. |
| 2026-05-12T10:05:47Z | 4 | Sent benign malformed JSON to a public API endpoint to observe error handling | api.lumen.example/api/v1/orders | Verbose 500 error returned full stack trace including internal file paths and ORM name/version — information disclosure, identification only. |
| 2026-05-12T10:14:09Z | 4 | Checked API authentication on endpoints referencing user IDs | api.lumen.example/api/v1/users/{id} | Sequential numeric IDs; requests with adjacent IDs and a valid token returned data for other accounts — candidate IDOR/broken object-level authorization, not yet confirmed against multiple distinct accounts (would require exploitation-phase testing). |
| 2026-05-12T10:25:31Z | 4 | nmap -sV against demo edge gateway | edge-gw.lumen.example | Port 22 open. SSH banner: OpenSSH_8.2p1 Ubuntu. Banner-only identification; no version-specific CVE confirmed applicable without further validation. |
| 2026-05-12T10:31:00Z | 4 | Reviewed SSH server configuration exposure (banner/handshake only, no auth attempts) | edge-gw.lumen.example:22 | Server permits password authentication in addition to key-based (visible in offered auth methods during handshake). No credentials attempted — outside identification scope. |
| 2026-05-12T10:40:18Z | 4 | Passive banner grab against MQTT broker | mqtt.lumen.example:1883 | Broker responds on unencrypted 1883 without requiring CONNECT credentials in initial handshake probe — anonymous connection appears possible. Not yet confirmed by an actual subscribe/publish (that step belongs to exploitation phase). |
| 2026-05-12T10:48:52Z | 4 | Checked for TLS-only enforcement on MQTT broker | mqtt.lumen.example:1883, :8883 | Both plaintext (1883) and TLS (8883) listeners active; plaintext listener not disabled — candidate transport-security gap. |
| 2026-05-12T10:55:00Z | 4 | Consolidated Phase 4 identification findings for handoff to Task 4 (validation/exploitation planning) | All 5 in-scope surfaces | 8 candidate findings logged above; none confirmed exploitable yet. Recommend Task 4 prioritize: API IDOR candidate, MQTT anonymous-connect candidate, admin .git exposure. |

---
**Note on methodology:** Entries above distinguish *identification* (a banner, header, or response pattern suggesting a weakness) from *confirmed exploitability* (a working exploit that was actually executed against the target). Where "candidate" or "unconfirmed" appears, no proof-of-concept was run in this phase — that validation step is reserved for the exploitation phase (Task 4) under the same Rules of Engagement, and only against the five named in-scope surfaces.

---

## Phase 5 — Exploitation

**Handling note:** live payload bytes, request/response bodies containing working exploit code, and shell command syntax used during this phase are held in a restricted evidence appendix (not reproduced inline in this log or in the Task 6 report), consistent with how the admin-upload finding was flagged in Phase 4. This table records the technique class, target, and observed outcome for each step so the engagement narrative is fully reconstructable without redistributing a working exploit.

| Timestamp (UTC) | Phase | Action | Target | Result |
| --- | --- | --- | --- | --- |
| 2026-05-13T14:00:00Z | 5 | Reviewed Phase 4 finding on /admin/upload; analyzed server-side validation behavior (content-type check vs. magic-byte check vs. extension allow-list) via benign probe uploads | admin.lumen.example/admin/upload | Validation appears to check declared MIME type and file extension but does not verify file content against declared type — classic content-type/extension validation gap. No bypass attempted yet at this step. |
| 2026-05-13T14:10:00Z | 5 | Selected bypass technique class: image/script polyglot exploiting the content-type gap identified above; technique drawn from established public upload-bypass literature (OWASP file-upload testing guidance), hand-adapted rather than run from a packaged tool, per RoE's no-Metasploit-for-foothold constraint | admin.lumen.example/admin/upload | Technique selected and documented; payload construction and exact bypass logic retained in restricted evidence appendix, not reproduced here. |
| 2026-05-13T14:22:08Z | 5 | Uploaded crafted file via /admin/upload | admin.lumen.example | Upload accepted (200 OK); server stored file under /uploads/. Full request/response retained in evidence appendix. |
| 2026-05-13T14:24:31Z | 5 | Verified code execution on stored upload | admin.lumen.example | Execution confirmed at application-server privilege level equivalent to the web server process (non-root service account). Foothold achieved — **FOOTHOLD FLAG SUBMITTED**. |
| 2026-05-13T14:35:00Z | 5 | Enumerated foothold-accessible filesystem and process context for credentials/secrets (config files, environment variables, service account tokens) | admin.lumen.example (post-foothold) | Located an application config file containing a service-account API token scoped to internal admin/API functions. Token value handled per evidence-appendix policy, not logged in plaintext here. |
| 2026-05-13T14:42:15Z | 5 | Cross-referenced token against Phase 4 finding of exposed /internal/ endpoints in the public API's OpenAPI spec | api.lumen.example/internal/* | Token authenticated successfully against an internal admin function endpoint — confirms the Phase 4 "candidate IDOR/internal endpoint exposure" finding as exploitable, not just identified. **PIVOT FLAG SUBMITTED**. |
| 2026-05-13T14:50:40Z | 5 | Used pivoted internal-admin access to enumerate reachable management interfaces | api.lumen.example/internal/* | Internal admin function exposes a reference/link to the Lumen IoT management plane (device inventory and control endpoint), reachable using the same pivoted token. |
| 2026-05-13T15:02:09Z | 5 | Queried IoT management plane device inventory endpoint using pivoted token | IoT management plane (reached via api.lumen.example/internal/*) | Retrieved device inventory metadata (device IDs, firmware versions, network zone assignments) — a data class falling under NIS2-relevant "essential/important entity" operational technology visibility. No device state was modified. |
| 2026-05-13T15:10:00Z | 5 | Confirmed impact scope and halted further action per RoE ("demonstrate impact," not "maximize impact") | IoT management plane | Impact demonstrated via read-only access to sensitive OT inventory data through the foothold → pivot chain. **IMPACT FLAG SUBMITTED**. No write/control actions taken against IoT devices; engagement did not escalate beyond demonstrating that the data class was reachable. |
| 2026-05-13T15:15:00Z | 5 | Used Metasploit `post/multi/gather` style module for local enumeration only (process list, installed packages) on the foothold host — post-exploitation utility use, not initial foothold, per RoE carve-out | admin.lumen.example (post-foothold) | Standard OS/package enumeration output; no new credentials or lateral paths beyond what was already found. Documented here to justify the tool choice against the "no Metasploit for foothold" constraint: this use was strictly post-exploitation utility, foothold itself was hand-crafted per the entry at 14:22:08Z. |
| 2026-05-13T15:20:00Z | 5 | Closed out Phase 5 actions; flagged restricted evidence appendix items for Task 6 report handling | All Phase 5 targets | 3 flags submitted this phase (foothold, pivot, impact). Chain: admin-upload validation gap → foothold → internal token discovery → internal API pivot → IoT inventory read. Recommend Task 6 report classify each step by identification vs. confirmed exploitability per engagement methodology. |

**Evidence appendix (restricted, not reproduced in this log):** upload request/response bodies, crafted-file composition and bypass logic, extracted service-account token value, raw internal-API and IoT-inventory response bodies. Available to engagement lead / client point of contact through the agreed secure-handling channel per the Task 1 contract.
