#!/bin/bash

# Read through flags (Simulation of aggregation)
# Cerebra exposed an open .git repository, had breached credentials in CloudLeak 2023, 
# and has an IP actively exhibiting C2 callbacks (Score 9). 
# Yume exposed a single API key in an old commit.
# Verdant exposed an internal portal path.

# Output the most exposed estate
echo "Cerebra"

# Output the single worst asset
echo "127.22.0.10"
